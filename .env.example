# Cloudinary Configuration for Video Upload
# Get these values from your Cloudinary Dashboard: https://cloudinary.com/console

# Your Cloudinary cloud name
NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME=your_cloud_name

# Upload preset name (must be configured in Cloudinary)
# Make sure to set it as "unsigned" in Cloudinary settings
NEXT_PUBLIC_CLOUDINARY_UPLOAD_PRESET=your_upload_preset

# Optional: API Key (if you need signed uploads)
# CLOUDINARY_API_KEY=your_api_key

# Optional: API Secret (if you need signed uploads)
# CLOUDINARY_API_SECRET=your_api_secret